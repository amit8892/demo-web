name: Deploy Website

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Update website
        run: |
          cd /var/www/demo-web
          git fetch origin main
          git reset --hard origin/main
